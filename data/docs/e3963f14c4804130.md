On this page

Last updated on **Jun 8, 2026**

Databricks provides centralized identity management for users, groups, and service principals across your account and workspaces. Identity management in Databricks enables you to control who can access your workspaces, data, and compute resources, with flexible options for syncing identities from your identity provider.

For an opinionated perspective on how to best configure identity in Databricks, see [Identity best practices](/aws/en/admin/users-groups/best-practices).

To manage access for users, service principals, and groups, see [Authentication and access control](/aws/en/security/auth/).

## Databricks identities[​](#databricks-identities "Direct link to databricks-identities")

Databricks supports three types of identities for authentication and access control:

| Identity type | Description |
| --- | --- |
| [Users](/aws/en/admin/users-groups/users) | User identities recognized by Databricks and represented by email addresses. |
| [Service principals](/aws/en/admin/users-groups/service-principals) | Identities for use with jobs, automated tools, and systems such as scripts, apps, and CI/CD platforms. |
| [Groups](/aws/en/admin/users-groups/groups) | A collection of identities used by admins to manage group access to workspaces, data, and other securable objects. All Databricks identities can be assigned as members of groups. |

A Databricks account can have a maximum of 10,000 combined users and service principals, along with up to 5,000 groups.
Each workspace also can have a maximum of 10,000 combined users and service principals as members, along with up to 5,000 groups.

## Who can manage identities in Databricks?[​](#who-can-manage-identities-in-databricks "Direct link to who-can-manage-identities-in-databricks")

To manage identities in Databricks, you must have one of the following roles:

| Role | Capabilities |
| --- | --- |
| Account admins | * Add, update, and delete users, service principals, and groups in the account. * Assign admin roles and grant users access to workspaces. * Automatically have group manager role on all groups and service principal manager role on all service principals in the account. |
| Workspace admins | * Add users, service principals, and groups to the Databricks account. * Cannot update or delete users or service principals in the account. * Grant users, service principals, and groups access to their workspaces. * Manage legacy [workspace-local groups](/aws/en/admin/users-groups/workspace-local-groups). * Automatically have group manager role on groups they create and service principal manager role on service principals they create. |
| Group managers | * Manage group membership and delete groups. * Assign the group manager role to other users. |
| Service principal managers | * Add, update, and remove roles on service principals. |

## Identity management workflows[​](#identity-management-workflows "Direct link to Identity management workflows")

note

Most workspaces are enabled for identity federation by default. Identity federation allows you to manage identities centrally at the account level and assign them to workspaces. This page assumes your workspace has identity federation enabled. If you have a legacy workspace without identity federation, see [Legacy workspaces without identity federation](#legacy-non-federated-workspaces).

### Identity federation[​](#identity-federation "Direct link to identity-federation")

Databricks enables identity federation by default for all new workspaces, and you cannot disable it.

In an identity federated workspace, when you add a user, service principal, or group in workspace admin settings, you can select from identities that exist in your account. In a non-identity federated workspace, you do not have the option to add users, service principals, or groups from your account.

To check if your workspace has identity federation enabled, look for **Identity federation: Enabled** on the workspace page in the account console. To enable identity federation for an older workspace, an account admin must enable the workspace for Unity Catalog by assigning a Unity Catalog metastore. See [Enable a workspace for Unity Catalog](/aws/en/data-governance/unity-catalog/enable-workspaces).

### Sync identities from your identity provider[​](#sync-identities-from-your-identity-provider "Direct link to sync-identities-from-your-identity-provider")

Preview

This feature is in [Public Preview](/aws/en/release-notes/release-types).

Databricks recommends syncing identities using automatic identity management. Automatic identity management supports Microsoft Entra ID and Okta.

Using automatic identity management, you can directly search for users, service principals, and groups in workspace admin settings and add them to your workspace and to the Databricks account. Databricks uses your identity provider as the source of record, so any changes to users or group memberships are respected in Databricks. For detailed instructions, see [Automatic identity management](/aws/en/admin/users-groups/automatic-identity-management/).

If automatic identity management is not available for your identity provider, use SCIM provisioning to sync users and groups automatically. For detailed instructions, see [Sync users and groups from your identity provider using SCIM](/aws/en/admin/users-groups/scim/).

### Assign identities to workspaces[​](#assign-identities-to-workspaces "Direct link to assign-identities-to-workspaces")

To enable a user, service principal, or group to work in a Databricks workspace, an account admin or workspace admin assigns them to the workspace. You can assign workspace access to any user, service principal, or group that exists in the account.

Workspace admins can also add a new user, service principal, or group directly to a workspace. This action automatically adds the identity to the account and assigns it to that workspace.

For detailed instructions, see:

* [Add users to a workspace](/aws/en/admin/users-groups/users#assign-workspace)
* [Add service principals to a workspace](/aws/en/admin/users-groups/manage-service-principals#add-sp)
* [Add groups to a workspace](/aws/en/admin/users-groups/manage-groups#add-groups-workspace)

### Share dashboards with account users[​](#share-dashboards-with-account-users "Direct link to Share dashboards with account users")

Users can share published dashboards with other users in the Databricks account, even if those users are not members of their workspace. Using automatic identity management, users can share dashboards with any user in their identity provider, which adds the user to the Databricks account upon login. Users in the Databricks account who are not members of any workspace are the equivalent of view-only users in other tools. They can view objects that have been shared with them, but they cannot modify objects. For more information, see [User and group management](/aws/en/ai-bi/admin/#manage-users-groups).

## Authentication[​](#authentication "Direct link to Authentication")

### Single sign-on (SSO)[​](#single-sign-on-sso "Direct link to Single sign-on (SSO)")

Single sign-on (SSO) enables you to authenticate your users using a third-party identity provider like Okta. To enable SSO, see [Configure SSO in Databricks](/aws/en/security/auth/single-sign-on/).

### Just-in-time provisioning[​](#just-in-time-provisioning "Direct link to Just-in-time provisioning")

When SSO is configured, you can configure just-in-time (JIT) provisioning to automatically create new user accounts from your identity provider upon their first login. See [Automatically provision users (JIT)](/aws/en/security/auth/jit).

## Access control[​](#access-control "Direct link to Access control")

Admins can assign roles, entitlements, and permissions to users, service principals, and groups to control access to workspaces, data, and other securable objects. For more information, see [Access control overview](/aws/en/security/auth/#acl).

## Legacy workspaces without identity federation[​](#legacy-workspaces-without-identity-federation "Direct link to legacy-workspaces-without-identity-federation")

For workspaces that aren't enabled for identity federation, workspace admins manage workspace users, service principals, and groups entirely within the scope of the workspace. Users and service principals added to non-identity federated workspaces are automatically added to the account. If the workspace user shares a username (that is, an email address) with an account user or admin that already exists, those users are merged into a single identity. Groups added to non-identity federated workspaces are legacy workspace-local groups that are not added to the account.

To enable identity federation for a legacy workspace, see [Identity federation](#enable-identity-federation).

## Additional resources[​](#additional-resources "Direct link to Additional resources")

* [Identity management best practices](/aws/en/admin/users-groups/best-practices) - Opinionated guidance on configuring identity in Databricks
* [Users](/aws/en/admin/users-groups/users) - Manage user identities
* [Service principals](/aws/en/admin/users-groups/service-principals) - Manage service principal identities
* [Groups](/aws/en/admin/users-groups/groups) - Manage group identities
* [Access control](/aws/en/security/auth/) - Manage permissions and access
* [SCIM provisioning](/aws/en/admin/users-groups/scim/) - Sync identities from your identity provider
* [Workspace-local groups](/aws/en/admin/users-groups/workspace-local-groups) - Manage legacy workspace-local groups