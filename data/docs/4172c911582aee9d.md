On this page

Last updated on **Jul 8, 2026**

This page describes how to use Supervisor Agent to create a multi-agent supervisor system that orchestrates AI agents and tools to work together on complex tasks. You can improve their coordination based on natural language feedback from your subject matter experts.

## What is Supervisor Agent?[​](#what-is-supervisor-agent "Direct link to what-is-supervisor-agent")

Use Supervisor Agent to create a supervisor system that coordinates Genie Agents, agent endpoints, Unity Catalog functions, MCP servers, and custom agents to work together to complete complex tasks across different, specialized domains. Supervisor Agent uses advanced AI orchestration patterns to manage agent interactions, task delegation, and result synthesis to deliver comprehensive solutions. It supports multiple languages.

Supervisor Agent builds the system for you and lets you improve it over time with human feedback. It is ideal for supporting the following use cases:

* Provide market analysis and insights by searching across research reports and usage data.
* Answer questions about internal processes and automate a ticket backlog for it.
* Speed up customer service by answering policy, FAQ, account, and other questions.

Supervisor Agent enables you to improve the supervisor's coordination quality and adjust agent behavior based on natural language feedback from your subject matter experts. Provide examples and guidelines to optimize the system's performance.

Supervisor Agent creates a comprehensive endpoint you can use downstream for your applications. For example, you can interact with the endpoint by submitting prompts in Playground or build a chat application using Databricks Apps. The supervisor has built-in access controls, so that its end users only access the subagents and data they have access to.

Supervisor Agent uses [default storage](/aws/en/storage/default-storage) to store temporary data transformations, model checkpoints, and internal metadata that power each agent. On agent deletion, all data associated with the agent is removed from default storage.

## Requirements[​](#requirements "Direct link to Requirements")

* A workspace that includes the following:
  + [Serverless compute](/aws/en/compute/serverless/) available (enabled by default in workspaces with Unity Catalog in a [supported region](/aws/en/resources/feature-region-support#serverless-availability)).
  + Unity Catalog enabled. See [Enable a workspace for Unity Catalog](/aws/en/data-governance/unity-catalog/enable-workspaces).
  + Access to [Model Serving](/aws/en/machine-learning/model-serving/).
  + Access to a [serverless usage policy](/aws/en/admin/usage/budget-policies) with a nonzero budget.
* A workspace in one of the [supported regions](/aws/en/resources/feature-region-support).
* You must have agents or tools ready to use. You must provide at least one [subagent](#subagents).
* End users of the supervisor agent need explicit access to interact with each subagent. See [Supported subagents and tools](#subagents) for required permissions.

### Supported subagents and tools[​](#supported-subagents-and-tools "Direct link to supported-subagents-and-tools")

warning

Executing arbitrary code in an agent tool can expose sensitive or private information that the agent has access to. Customers are responsible to run only trusted code, and implement guardrails and proper permissions to prevent unintended access to data.

When creating a supervisor, you must provide subagents for it to coordinate and grant end users explicit access to each one. The following table lists supported subagent types, how to create each, and the required end user permissions. The supervisor has built-in access controls, so that its end users only access the subagents and data they have access to. Without explicit access, the supervisor cannot return helpful responses from a subagent.

| Subagent type | How to create | Required end user permissions |
| --- | --- | --- |
| Genie Agent | [Create and manage a Genie Agent](/aws/en/genie/set-up) | Access to the Genie Agent and its underlying Unity Catalog objects. See [Share a Genie Agent](/aws/en/genie/set-up#share-a-genie-agent). |
| Published dashboard | [Create a dashboard](/aws/en/dashboards/tutorials/create-dashboard), then [Publish a dashboard](/aws/en/dashboards/share/share#publish-dashboard) | `CAN VIEW` on the published dashboard. See [Manage dashboard permissions](/aws/en/dashboards/share/share#permissions). |
| Knowledge Assistant agent endpoint | [Use Knowledge Assistant to create a high-quality chatbot over your documents](/aws/en/agents/agent-bricks/knowledge-assistant) | `CAN QUERY` on the agent endpoint. See [Manage permissions](/aws/en/agents/agent-bricks/knowledge-assistant#manage-permissions). |
| Model serving endpoint | [Create custom model serving endpoints](/aws/en/machine-learning/model-serving/create-manage-serving-endpoints) | `CAN QUERY` on the serving endpoint. See [Manage permissions on a model serving endpoint](/aws/en/machine-learning/model-serving/manage-serving-endpoints#permissions). |
| Unity Catalog function | [Create AI agent tools using Unity Catalog functions](/aws/en/agents/agent-framework/create-custom-tool) | `EXECUTE` on the Unity Catalog function. See [EXECUTE](/aws/en/data-governance/unity-catalog/access-control/privileges-reference#execute). |
| Unity Catalog table | [Databricks tables](/aws/en/tables/) | `SELECT` on the table, plus `USE CATALOG` and `USE SCHEMA` on its parent catalog and schema. See [Unity Catalog privileges reference](/aws/en/data-governance/unity-catalog/access-control/privileges-reference). |
| Unity Catalog volume | [What are Unity Catalog volumes?](/aws/en/volumes/) | `READ VOLUME` on the volume, plus `USE CATALOG` and `USE SCHEMA` on its parent catalog and schema. See [Unity Catalog privileges reference](/aws/en/data-governance/unity-catalog/access-control/privileges-reference). |
| AI Search index | [Create AI Search endpoints and indexes](/aws/en/ai-search/create-ai-search) | `USE CATALOG` on the parent catalog, `USE SCHEMA` on the parent schema, and `SELECT` on the AI Search index. See [Unity Catalog privileges reference](/aws/en/data-governance/unity-catalog/access-control/privileges-reference). |
| Supervisor Agent | [Use Supervisor Agent to create a coordinated multi-agent system](/aws/en/agents/agent-bricks/multi-agent-supervisor) | `CAN QUERY` on the Supervisor Agent. See  [Step 4: Manage permissions](#permissions). |
| Web search | Built-in capability — no setup required. Supervisor Agent uses Foundation Model APIs for [web search](/aws/en/machine-learning/model-serving/web-search). See [Limitations](#limitations) for workspace eligibility. | No additional permissions. The supervisor always prompts the end user to approve each web search invocation before sending the query to the public web, and returns a synthesized answer with source citations. |
| External MCP server | [Connect agents to third-party tools with MCP Services](/aws/en/agents/agent-framework/mcp-services), or [install one from Databricks Marketplace](/aws/en/marketplace/get-started-consumer#install-mcp). | `USE CONNECTION` on the Unity Catalog connection. See [USE CONNECTION](/aws/en/data-governance/unity-catalog/access-control/privileges-reference#use-connection). |
| Custom MCP server | [Host your own MCP server](/aws/en/agents/mcp/custom-mcp) | `CAN_USE` on the Databricks app. See [Authentication for AI agents](/aws/en/agents/agent-framework/agent-authentication). |
| Custom agent | [Author an AI agent and deploy it on Databricks Apps](/aws/en/agents/agent-framework/author-agent) | `CAN_USE` on the Databricks app. See [Authentication for AI agents](/aws/en/agents/agent-framework/agent-authentication). |

## Create a multi-agent supervisor system[​](#create-a-multi-agent-supervisor-system "Direct link to Create a multi-agent supervisor system")

Go to  **Agents** in the left navigation pane of your workspace. Click **Create Agent**, and select **Supervisor Agent**.

## Step 1: Configure your supervisor[​](#step-1-configure-your-supervisor "Direct link to Step 1: Configure your supervisor")

Configure your supervisor and add the agents it will coordinate.

1. Under **Tools and sub-agents** in the left side pane, add your tools and subagents. Click on the type of tool/agent you'd like to add, then select from the dropdown that appears. Or, use the search bar to select tools/agents. You can select up to 50 different agents and tools.
2. For better results, provide a description for each tool/sub-agent. Click on the tool/subagent to adjust the description. The supervisor uses the information in the description to help it coordinate agents. Provide as much detail as possible to help improve its task delegation.
3. To manage permissions for each tool/agent, hover over the tool/agent tile and click . This opens that tool/agent in Databricks, and you can configure permissions from there. See [Supported subagents and tools](#subagents) for required end user permissions.

   note

   If the end user does not have access to any subagents, the supervisor will end the conversation. If the end user has access to some but not all subagents, the supervisor will redirect the conversation away from subagents the user cannot access.
4. (Optional) In the **Instructions** field, specify guidelines for how the supervisor should respond.
5. (Optional) In the **Description** field, provide a summary of what this supervisor does. This description is shown to users and used for search.

## Step 2: Test your supervisor agent[​](#step-2-test-your-supervisor-agent "Direct link to Step 2: Test your supervisor agent")

After your supervisor finishes initializing, test it to see how well the supervisor coordinates multiple agents to handle complex tasks. In the right side pane, chat with the agent to evaluate its responses.

1. (Optional) You can also test the agent in [AI Playground](/aws/en/large-language-models/ai-playground). Click **Open in Playground**. This opens up AI Playground with your supervisor endpoint connected. If you have AI assistive features enabled, you can enable **AI Judge** and **Synthetic task generation** to help you evaluate your supervisor.
2. Enter a complex question or task for your supervisor.
3. Evaluate its response. Ensure that the supervisor successfully delegates tasks to the right agents.
4. While interacting with the supervisor, you may be prompted to **Add guidelines** to help improve its response. Provide response guidelines and regenerate the response.
5. Based on your agent's responses, adjust the **Description** and **Instructions** fields on the left side panel to improve its configuration.

If you're satisfied with your supervisor's performance, continue using the supervisor as-is.

## Step 3: Improve the supervisor[​](#step-3-improve-the-supervisor "Direct link to Step 3: Improve the supervisor")

Supervisor Agent can adjust the supervisor's behavior based on natural language feedback. Gather feedback from experts through the configuration page to improve your supervisor's coordination quality. Collecting labeled data for your supervisor can improve its performance. Supervisor Agent will retrain and optimize the supervisor from the new data.

In the **Examples** tab, add questions and task scenarios for the supervisor.

1. Add questions to label:

   1. Click **+ Add** to add a question.
   2. In the **Add a question** modal, enter your question.
   3. Click **Add**. The question should appear in the UI.
   4. Repeat until you’ve added all the questions you want to evaluate.
   5. To delete a question, click the kebab menu, then **Delete**.
2. After you've finished adding your questions, you can share the agent with others to review to help you build a high-quality labeled dataset. Share a link to your Supervisor Agent configuration page to gather feedback from experts.
3. Ensure the experts have access to the supervisor agent and the appropriate subagents:

   1. In the upper right-hand corner, click the kebab menu  to [manage permissions](#permissions). Grant the experts CAN\_MANAGE permissions so they can access the agent and provide feedback.
   2. Ensure the SME has access to each subagent. See [Supported subagents and tools](#subagents) for the required permissions per subagent type.

   If the SME does not have access to any subagents, the supervisor will end the conversation. If the end user has access to some but not all subagents, the supervisor will redirect the conversation away from subagents the user cannot access.
4. To label data, click on a question and add **Guidelines** in the panel that appears. Guidelines apply right after they are saved.
5. Test the agent again in the configuration page or AI Playground to see its improved performance. If needed, add more questions and guidelines to continue improving the behavior.

## Step 4: Manage permissions[​](#-step-4-manage-permissions "Direct link to -step-4-manage-permissions")

By default, only agent authors and workspace admins have permissions to the agent. To allow other users to edit or query your agent, you need to explicitly grant them permission.

To manage permissions on your agent:

1. Open your agent on the **Agents** page.
2. At the top, click the  kebab menu.
3. Click **Manage permissions**.
4. In the **Permission Settings** window, select the user, group, or service principal.
5. Select the permission to grant:
   * **Can Manage**: Allows managing the agent, including setting permissions, editing the agent configuration, and improving its quality.
   * **Can Query**: Allows querying the agent endpoint in [AI Playground](/aws/en/large-language-models/ai-playground) and through the API. Users with only this permission cannot view or edit the agent on the Agents page.
6. Click **Add**.
7. Click **Save**.

note

For agent endpoints created **before September 16, 2025**, you can grant 