A Genie Agent can analyze the unstructured files stored in Unity Catalog volumes, such as PDFs, Word documents, slide decks, and images. An agent author attaches up to 10 volumes, and business users ask questions about their content alongside the agent's structured data in a single conversation.

Beta

This feature is in [Beta](/aws/en/release-notes/release-types). To use it, a workspace admin must turn on **Analyze Files in Volumes with Genie Agents** from the **Previews** page. See [Manage Databricks previews](/aws/en/admin/workspace-settings/manage-previews).

note

Genie Agents were formerly known as Genie Spaces.

## What you can ask a Genie Agent about your files

By attaching volumes to an agent, business users can combine data from documents such as support tickets, sales call logs, and policy files with data from the agent's structured tables. Users can then answer questions such as:

* Which support issues are taking the longest to resolve, and what are customers saying in escalation requests?
* Why are late-stage sales opportunities stalling, and what objections appear most often in sales call logs?
* Which regions are underperforming, and what internal escalation policies apply?

To blend a local CSV or Excel file into a single conversation instead of analyzing files stored in volumes, see [Upload a file to a Genie Agent](/aws/en/genie-agents/file-upload).

## How a Genie Agent analyzes files

When a user asks a question that requires context from a volume, the Genie Agent retrieves the file content that is most relevant to the question and adds it to the agent's context. The agent then reasons over that content together with any structured data to generate an answer with citations. These citations can point to the specific page of the source file (Beta).

File analysis runs only in Agent mode, which uses multi-step reasoning to retrieve and analyze the relevant files. See [Agent mode](/aws/en/genie-agents/concepts#agent-mode).

The Genie Agent always uses the permissions of the user who asks the question. A user who does not have at least `READ VOLUME` permission on an attached volume receives an error. Users can ask questions across a specific volume, multiple volumes, and structured data in the same conversation.

## Requirements

The following requirements apply to the agent and its users:

* [Unity Catalog](/aws/en/data-governance/unity-catalog/) enabled in your workspace.
* Agent mode available in your region. See [Regional availability](/aws/en/genie-agents/concepts#regional-availability).
* A well-prepared Genie Agent with clear instructions and table metadata. See [Curate an effective Genie Agent](/aws/en/genie-agents/best-practices).
* One or more Unity Catalog volumes that contain relevant files.
* Both the **Workspace access** and **Databricks SQL access** entitlements for every user of the agent. Users can use the rest of Genie without **Workspace access**, but this feature requires both. See [Manage entitlements](/aws/en/security/auth/entitlements).
* At least `READ VOLUME` permission on the attached volumes for every user of the agent.

## Attach volumes to a Genie Agent

Before you begin, review the [Requirements](#requirements).

To attach volumes to a Genie Agent:

1. Open the Genie Agent that you want to add files to.
2. Open the **Sources** tab, then click **Add**.
3. Select one or more volumes. The Genie Agent processes the entire volume. You cannot attach a subset of the folders or files in a volume.
4. Add a clear description in the **Description** field. See [Optimize your agent for file questions](#optimize).
5. Optionally, enable content search for the volumes you attached. See [Enable content search for attached volumes](#content-search).

### Enable content search for attached volumes

Databricks recommends enabling content search for the volumes you attach. Content search prepares and indexes your files so that the Genie Agent can use them more effectively, which improves its ability to reason about a larger set of files and answer questions with lower latency. An indexed volume can contain up to 10,000 files of up to 50 MB each. See [Content search for Unity Catalog volumes](/aws/en/volumes/content-search).

Content search is available only in certain regions, and depending on the region, an account admin might need to enable cross-Geo processing first. In regions where content search isn't available, attach volumes without it, subject to the limits in [Limitations for volumes without content search](#non-search-limits). See [Supported regions](/aws/en/volumes/content-search#content-search-regions).

## Optimize your agent for file questions

After you attach your volumes, follow these recommendations to help the Genie Agent use their content:

* **Add a clear description**: Describe what content the volume contains, how it is organized, and how the agent should use it. The agent uses this description to retrieve relevant files.
* **Avoid duplicate content**: Attaching multiple volumes that contain overlapping information makes it harder for the agent to retrieve relevant documents. The same applies to individual files within a volume.
* **Avoid irrelevant files**: Include only files that are relevant to the agent's domain. Irrelevant files can confuse the agent.
* **Use clear file names**: Use descriptive file names so the agent can distinguish between files.

## Ask questions about files

To ask questions about files in a Genie Agent:

1. Open a Genie Agent that has volumes attached.
2. Turn on Agent mode in the chat box. You must use Agent mode to answer questions about files.
3. Enter a question and send your prompt. Agent mode retrieves context from the relevant volume, and then generates an answer after the analysis is complete.

## Limitations

The following limitations apply:

* You can attach a maximum of 10 volumes to a Genie Agent.
* This feature is not available on AWS GovCloud.
* Consumer-only, Databricks SQL-only, and account-only users cannot use this feature. Every user of the agent must have both the **Workspace access** and **Databricks SQL access** entitlements.
* On volumes that don't have content search enabled, a Genie Agent can retrieve context from up to 5 files per question. See [Limitations for volumes without content search](#non-search-limits).
* The Genie Agent cannot analyze external files, such as files in Google Drive or SharePoint.
* Web-search tools are not available.

### Limitations for volumes without content search

Databricks recommends enabling content search on the volumes you attach. If you don't enable content search for an attached volume, a Genie Agent can retrieve context from up to 5 files per question. Additionally, the following limits apply to the files that it can read:

| Property | Limit |
| --- | --- |
| Total files per volume | Up to 500 files. TXT and MD files do not count toward this limit. Questions directed toward volumes with more than 500 files return an error. |
| Supported file types | PDF, JPG, JPEG, PNG, TIFF, TIF, DOC, DOCX, PPT, or PPTX |
| Maximum file size | Up to 10 MB |

←✕

| Property | Limit |
| --- | --- |
| Total files per volume | Up to 500 files. TXT and MD files do not count toward this limit. Questions directed toward volumes with more than 500 files return an error. |
| Supported file types | PDF, JPG, JPEG, PNG, TIFF, TIF, DOC, DOCX, PPT, or PPTX |
| Maximum file size | Up to 10 MB |

The Genie Agent ignores unsupported file types and files that exceed the maximum file size. It continues to answer questions using the other supported files in the volume.

## Frequently asked questions

The following questions cover common topics for analyzing files in volumes.

### How is this feature billed?

Billing depends on whether the attached volume has content search enabled. In both cases, the agent reasons over the retrieved content, which is billed as part of standard Genie usage. See [Genie pricing](https://www.databricks.com/product/pricing/genie).

* **Volumes without content search**: During the Beta, users are billed only for the agent's reasoning over the retrieved content.
* **Volumes with content search**: The agent queries the existing index instead of parsing files at query time, so no Foundation Model Serving costs apply to retrieval. Ingestion and query-serving costs apply instead. See [How content search is billed](/aws/en/volumes/content-search#cost).

### When should I use this feature instead of a Multi-Agent Supervisor?

Attach volumes to a Genie Agent when you want an agent to answer questions across structured and unstructured data within a specific domain. Build a custom supervisor agent only when you need fine-grained control over agent behavior, such as model selection, chunking strategy, or orchestrating multiple sub-agents.

### When should I use this feature instead of Genie One?

Attach volumes to a Genie Agent when you want to build a specialized agent that answers questions in a specific domain. Use [Genie One](/aws/en/genie-one/) as a unified entry point for all questions about your structured data. Genie One can call a Genie Agent to answer questions about files.

### Can I analyze files in volumes through the API?

Yes. You can analyze files in volumes through the Agent mode APIs. See the [Agent mode API reference](https://docs.databricks.com/api/genie/v1/agent-mode-create-response).

## Additional resources

See the following resources for related information:

* [Genie Agents concepts](/aws/en/genie-agents/concepts)
* [Create and manage a Genie Agent](/aws/en/genie-agents/set-up)
* [Curate an effective Genie Agent](/aws/en/genie-agents/best-practices)
* [Upload a file to a Genie Agent](/aws/en/genie-agents/file-upload)
* [Content search for Unity Catalog volumes](/aws/en/volumes/content-search)