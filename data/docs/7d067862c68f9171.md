This page is a reference for Unity Catalog privileges and the securable objects they apply to.

For detailed descriptions of each securable object type, see [Unity Catalog securable objects reference](/aws/en/data-governance/unity-catalog/securable-objects). To learn how to grant privileges in Unity Catalog, see [Show, grant, and revoke privileges](/aws/en/data-governance/unity-catalog/manage-privileges/#grant).

note

This page refers to the Unity Catalog privileges and inheritance model in Privilege Model version 1.0. If you created your Unity Catalog metastore during the public preview (before August 25, 2022), you might be on an earlier privilege model that doesn't support the current inheritance model. You can upgrade to Privilege Model version 1.0 to get [privilege inheritance](/aws/en/data-governance/unity-catalog/access-control/permissions-concepts#inheritance). See [Upgrade to privilege inheritance](/aws/en/archive/unity-catalog/upgrade-privilege-model).

## Securable objects in Unity Catalog

A securable object is an object defined in the Unity Catalog [metastore](/aws/en/data-governance/unity-catalog/securable-objects#metastore) on which privileges can be granted to a principal (user, service principal, or group). Securable objects in Unity Catalog are hierarchical, from the [metastore](/aws/en/data-governance/unity-catalog/securable-objects#metastore) at the top, through [catalogs](/aws/en/data-governance/unity-catalog/securable-objects#catalog) and [schemas](/aws/en/data-governance/unity-catalog/securable-objects#schema), down to the data objects they contain ([tables](/aws/en/data-governance/unity-catalog/securable-objects#table), [views](/aws/en/data-governance/unity-catalog/securable-objects#view), [volumes](/aws/en/data-governance/unity-catalog/securable-objects#volume), [functions](/aws/en/data-governance/unity-catalog/securable-objects#function), and [models](/aws/en/data-governance/unity-catalog/securable-objects#model)). Additional securable objects govern access to external storage, external services, and OpenSharing.

For detailed descriptions of each securable object type, see [Unity Catalog securable objects reference](/aws/en/data-governance/unity-catalog/securable-objects).

## What privileges apply to each securable object?

The following table lists the privileges that apply to each securable object in Unity Catalog. To learn how to grant privileges in Unity Catalog, see [Show, grant, and revoke privileges](/aws/en/data-governance/unity-catalog/manage-privileges/#grant).

| Securable | Privileges |
| --- | --- |
| [Metastore](/aws/en/data-governance/unity-catalog/securable-objects#metastore) | `CREATE CATALOG`, `CREATE CLEAN ROOM`, `CREATE CONNECTION`, `CREATE EXTERNAL LOCATION`, `CREATE EXTERNAL METADATA`, `CREATE PROVIDER`, `CREATE RECIPIENT`, `CREATE SHARE`,`CREATE SERVICE CREDENTIAL`, `CREATE STORAGE CREDENTIAL`, `MANAGE ALLOWLIST`, `READ METADATA`, `SET SHARE PERMISSION`, `USE MARKETPLACE ASSETS`, `USE PROVIDER`, `USE RECIPIENT`, `USE SHARE`  When granted on the [metastore](/aws/en/data-governance/unity-catalog/securable-objects#metastore), `READ METADATA` inherits down to all objects in the metastore. This is unlike other metastore-level privileges, which don't follow privilege inheritance. |
| [Catalog](/aws/en/data-governance/unity-catalog/securable-objects#catalog) | `ALL PRIVILEGES`, `APPLY TAG`, `BROWSE`, `CREATE SCHEMA`, `USE CATALOG`  All users have `USE CATALOG` on the `main` catalog by default.  The following privileges apply to securable objects in a catalog. You can grant these privileges at the catalog level to apply them to current and future objects in the catalog.  `CREATE FEATURE`, `CREATE FUNCTION`, `CREATE SECRET`, `CREATE TABLE`, `CREATE MATERIALIZED VIEW`, `CREATE MODEL`, `CREATE SERVICE`, `CREATE VOLUME`, `EXTERNAL USE SCHEMA`, `READ FEATURE`, `READ METADATA`, `READ SECRET`, `READ VOLUME`, `REFERENCE SECRET`, `REFRESH`, `WRITE SECRET`, `WRITE VOLUME`, `EXECUTE`, `MANAGE`, `MODIFY`, `SELECT`, `USE SCHEMA` |
| [Schema](/aws/en/data-governance/unity-catalog/securable-objects#schema) | `ALL PRIVILEGES`, `APPLY TAG`, `CREATE FEATURE`, `CREATE FUNCTION`, `CREATE SECRET`, `CREATE TABLE`, `CREATE MODEL`, `CREATE SERVICE`, `CREATE VOLUME`, `CREATE MATERIALIZED VIEW`, `MANAGE`, `READ METADATA`, `EXTERNAL USE SCHEMA`, `USE SCHEMA`  The following privileges apply to securable objects within a schema. You can grant these privileges at the schema level to apply them to current and future objects within the schema.  `EXECUTE`, `MODIFY`, `READ FEATURE`, `READ SECRET`, `READ VOLUME`, `REFERENCE SECRET`, `REFRESH`, `SELECT`, `WRITE SECRET`, `WRITE VOLUME` |
| [Table](/aws/en/data-governance/unity-catalog/securable-objects#table) | `ALL PRIVILEGES`, `APPLY TAG`, `MANAGE`, `MODIFY`, `READ METADATA`, `SELECT` |
| [Materialized view](/aws/en/data-governance/unity-catalog/securable-objects#materialized-view) | `ALL PRIVILEGES`, `APPLY TAG`, `MANAGE`, `READ METADATA`, `REFRESH`, `SELECT` |
| [View](/aws/en/data-governance/unity-catalog/securable-objects#view) | `ALL PRIVILEGES`, `APPLY TAG`, `MANAGE`, `READ METADATA`, `SELECT` |
| [Volume](/aws/en/data-governance/unity-catalog/securable-objects#volume) | `ALL PRIVILEGES`, `APPLY TAG`, `MANAGE`, `READ METADATA`, `READ VOLUME`, `WRITE VOLUME` |
| [Secret](/aws/en/data-governance/unity-catalog/securable-objects#secret) | `ALL PRIVILEGES`, `MANAGE`, `READ METADATA`, `READ SECRET`, `REFERENCE SECRET`, `WRITE SECRET`  `CREATE SECRET` is granted at the catalog or schema level. `BROWSE` does not apply to secrets. |
| [Feature](/aws/en/data-governance/unity-catalog/securable-objects#feature) | `ALL PRIVILEGES`, `MANAGE`, `READ FEATURE`, `READ METADATA` |
| [External location](/aws/en/data-governance/unity-catalog/securable-objects#external-location) | `ALL PRIVILEGES`, `BROWSE`, `CREATE EXTERNAL TABLE`, `CREATE EXTERNAL VOLUME`, `CREATE FOREIGN SECURABLE`, `CREATE MANAGED STORAGE`, `EXTERNAL USE LOCATION`, `MANAGE`, `READ FILES`, `READ METADATA`, `WRITE FILES` |
| [External metadata](/aws/en/data-governance/unity-catalog/securable-objects#external-metadata) | `ALL PRIVILEGES`, `APPLY TAG`, `BROWSE`, `MANAGE`, `MODIFY`, `READ METADATA` |
| [Service credential](/aws/en/data-governance/unity-catalog/securable-objects#service-credential) | `ALL PRIVILEGES`, `ACCESS`, `CREATE CONNECTION`, `MANAGE`, `READ METADATA` |
| [Storage credential](/aws/en/data-governance/unity-catalog/securable-objects#storage-credential) | `ALL PRIVILEGES`, `CREATE EXTERNAL LOCATION`, `CREATE EXTERNAL TABLE`, `MANAGE`, `READ FILES`, `READ METADATA`, `WRITE FILES` |
| [Connection](/aws/en/data-governance/unity-catalog/securable-objects#connection) | `ALL PRIVILEGES`, `CREATE FOREIGN CATALOG`, `MANAGE`, `READ METADATA`, `USE CONNECTION` |
| [Function](/aws/en/data-governance/unity-catalog/securable-objects#function) | `ALL PRIVILEGES`, `APPLY TAG` (models only), `CREATE MODEL VERSION` (models only), `EXECUTE`, `MANAGE`, `READ METADATA` |
| [Model](/aws/en/data-governance/unity-catalog/securable-objects#model) | Registered models are a type of [function](/aws/en/data-governance/unity-catalog/securable-objects#function). |
| [Service](/aws/en/data-governance/unity-catalog/securable-objects#service) | `ALL PRIVILEGES`, `EXECUTE`, `MANAGE`, `READ METADATA` |
| [Share](/aws/en/data-governance/unity-catalog/securable-objects#share) | `SELECT` (Can be granted to `RECIPIENT`) |
| [Recipient](/aws/en/data-governance/unity-catalog/securable-objects#recipient) | None |
| [Provider](/aws/en/data-governance/unity-catalog/securable-objects#provider) | None |
| [Clean room](/aws/en/data-governance/unity-catalog/securable-objects#clean-room) | `ALL PRIVILEGES`, `BROWSE`, `EXECUTE CLEAN ROOM TASK`, `MANAGE`, `MODIFY CLEAN ROOM`, `READ METADATA` |

←✕

| Securable | Privileges |
| --- | --- |
| [Metastore](/aws/en/data-governance/unity-catalog/securable-objects#metastore) | `CREATE CATALOG`, `CREATE CLEAN ROOM`, `CREATE CONNECTION`, `CREATE EXTERNAL LOCATION`, `CREATE EXTERNAL METADATA`, `CREATE PROVIDER`, `CREATE RECIPIENT`, `CREATE SHARE`,`CREATE SERVICE CREDENTIAL`, `CREATE STORAGE CREDENTIAL`, `MANAGE ALLOWLIST`, `READ METADATA`, `SET SHARE PERMISSION`, `USE MARKETPLACE ASSETS`, `USE PROVIDER`, `USE RECIPIENT`, `USE SHARE`  When granted on the [metastore](/aws/en/data-governance/unity-catalog/securable-objects#metastore), `READ METADATA` inherits down to all objects in the metastore. This is unlike other metastore-level privileges, which don't follow privilege inheritance. |
| [Catalog](/aws/en/data-governance/unity-catalog/securable-objects#catalog) | `ALL PRIVILEGES`, `APPLY TAG`, `BROWSE`, `CREATE SCHEMA`, `USE CATALOG`  All users have `USE CATALOG` on the `main` catalog by default.  The following privileges apply to securable objects in a catalog. You can grant these privileges at the catalog level to apply them to current and future objects in the catalog.  `CREATE FEATURE`, `CREATE FUNCTION`, `CREATE SECRET`, `CREATE TABLE`, `CREATE MATERIALIZED VIEW`, `CREATE MODEL`, `CREATE SERVICE`, `CREATE VOLUME`, `EXTERNAL USE SCHEMA`, `READ FEATURE`, `READ METADATA`, `READ SECRET`, `READ VOLUME`, `REFERENCE SECRET`, `REFRESH`, `WRITE SECRET`, `WRITE VOLUME`, `EXECUTE`, `MANAGE`, `MODIFY`, `SELECT`, `USE SCHEMA` |
| [Schema](/aws/en/data-governance/unity-catalog/securable-objects#schema) | `ALL PRIVILEGES`, `APPLY TAG`, `CREATE FEATURE`, `CREATE FUNCTION`, `CREATE SECRET`, `CREATE TABLE`, `CREATE MODEL`, `CREATE SERVICE`, `CREATE VOLUME`, `CREATE MATERIALIZED VIEW`, `MANAGE`, `READ METADATA`, `EXTERNAL USE SCHEMA`, `USE SCHEMA`  The following privileges apply to securable objects within a schema. You can grant these privileges at the schema level to apply them to current and future objects within the schema.  `EXECUTE`, `MODIFY`, `READ FEATURE`, `READ SECRET`, `READ VOLUME`, `REFERENCE SECRET`, `REFRESH`, `SELECT`, `WRITE SECRET`, `WRITE VOLUME` |
| [Table](/aws/en/data-governance/unity-catalog/securable-objects#table) | `ALL PRIVILEGES`, `APPLY TAG`, `MANAGE`, `MODIFY`, `READ METADATA`, `SELECT` |
| [Materialized view](/aws/en/data-governance/unity-catalog/securable-objects#materialized-view) | `ALL PRIVILEGES`, `APPLY TAG`, `MANAGE`, `READ METADATA`, `REFRESH`, `SELECT` |
| [View](/aws/en/data-governance/unity-catalog/securable-objects#view) | `ALL PRIVILEGES`, `APPLY TAG`, `MANAGE`, `READ METADATA`, `SELECT` |
| [Volume](/aws/en/data-governance/unity-catalog/securable-objects#volume) | `ALL PRIVILEGES`, `APPLY TAG`, `MANAGE`, `READ METADATA`, `READ VOLUME`, `WRITE VOLUME` |
| [Secret](/aws/en/data-governance/unity-catalog/securable-objects#secret) | `ALL PRIVILEGES`, `MANAGE`, `READ METADATA`, `READ SECRET`, `REFERENCE SECRET`, `WRITE SECRET`  `CREATE SECRET` is granted at the catalog or schema level. `BROWSE` does not apply to secrets. |
| [Feature](/aws/en/data-governance/unity-catalog/securable-objects#feature) | `ALL PRIVILEGES`, `MANAGE`, `READ FEATURE`, `READ METADATA` |
| [External location](/aws/en/data-governance/unity-catalog/securable-objects#external-location) | `ALL PRIVILEGES`, `BROWSE`, `CREATE EXTERNAL TABLE`, `CREATE EXTERNAL VOLUME`, `CREATE FOREIGN SECURABLE`, `CREATE MANAGED STORAGE`, `EXTERNAL USE LOCATION`, `MANAGE`, `READ FILES`, `READ METADATA`, `WRITE FILES` |
| [External metadata](/aws/en/data-governance/unity-catalog/securable-objects#external-metadata) | `ALL PRIVILEGES`, `APPLY TAG`, `BROWSE`, `MANAGE`, `MODIFY`, `READ METADATA` |
| [Service credential](/aws/en/data-governance/unity-catalog/securable-objects#service-credential) | `ALL PRIVILEGES`, `ACCESS`, `CREATE CONNECTION`, `MANAGE`, `READ METADATA` |
| [Storage credential](/aws/en/data-governance/unity-catalog/securable-objects#storage-credential) | `ALL PRIVILEGES`, `CREATE EXTERNAL LOCATION`, `CREATE EXTERNAL TABLE`, `MANAGE`, `READ FILES`, `READ METADATA`, `WRITE FILES` |
| [Connection](/aws/en/data-governance/unity-catalog/securable-objects#connection) | `ALL PRIVILEGES`, `CREATE FOREIGN CATALOG`, `MANAGE`, `READ METADATA`, `USE CONNECTION` |
| [Function](/aws/en/data-governance/unity-catalog/securable-objects#function) | `ALL PRIVILEGES`, `APPLY TAG` (models only), `CREATE MODEL VERSION` (models only), `EXECUTE`, `MANAGE`, `READ METADATA` |
| [Model](/aws/en/data-governance/unity-catalog/securable-objects#model) | Registered models are a type of [function](/aws/en/data-governance/unity-catalog/securable-objects#function). |
| [Service](/aws/en/data-governance/unity-catalog/securable-objects#service) | `ALL PRIVILEGES`, `EXECUTE`, `MANAGE`, `READ METADATA` |
| [Share](/aws/en/data-governance/unity-catalog/securable-objects#share) | `SELECT` (Can be granted to `RECIPIENT`) |
| [Recipient](/aws/en/data-governance/unity-catalog/securable-objects#recipient) | None |
| [Provider](/aws/en/data-governance/unity-catalog/securable-objects#provider) | None |
| [Clean room](/aws/en/data-governance/unity-catalog/securable-objects#clean-room) | `ALL PRIVILEGES`, `BROWSE`, `EXECUTE CLEAN ROOM TASK`, `MANAGE`, `MODIFY CLEAN ROOM`, `READ METADATA` |

## Overview of privileges in Unity Catalog

The following table summarizes the capability each Unity Catalog privilege grants. For full descriptions, see [Detailed Unity Catalog privileges reference](#privilege-descriptions).

| Privilege | Allows you to |
| --- | --- |
| [ACCESS](#access) | Use a [service credential](/aws/en/data-governance/unity-catalog/securable-objects#service-credential) to access an external service. |
| [ALL PRIVILEGES](#all-privileges) | Perform all capabilities on an object and its children. `ALL PRIVILEGES` does not include the `EXTERNAL USE SCHEMA`, `EXTERNAL USE LOCATION`, `MANAGE`, or `READ METADATA` privileges. |
| [APPLY TAG](#apply-tag) | Add and edit tags on an object. For [tables](/aws/en/data-governance/unity-catalog/securable-objects#table) and [views](/aws/en/data-governance/unity-catalog/securable-objects#view), also enables column tagging. For regi